# 知乎OS

## 系统职责

本系统负责知乎内容生产、平台样本学习与账号数据验证。

当前系统内核升级为 `知乎OS Compiler V1`：

```text
Analyzer
↓
Production Card IR
↓
Writer Prompt
↓
Writer
↓
QA
↓
Feedback
```

核心原则：

```text
Reasoning First
Compiler First
决策层永远不讨论表达
表达层永远不修改决策
任何规则只有一个权威来源
系统只允许单向流动
```

Reasoning First 表示：任何自动化之前，先定义推导；任何字段设计之前，先定义思维。系统建设顺序必须是 Reasoning Grammar → Evidence Schema → Knowledge Engine → Parser → Automation。

详见 [`docs/知乎OS Compiler V1.md`](docs/知乎OS%20Compiler%20V1.md)。

系统设计边界详见 [`docs/00-设计原则.md`](docs/00-设计原则.md)。

当前系统进入第二阶段：

```text
平台高表现样本负责发现规律
↓
本账号发布数据负责验证规律
↓
ACTIVE规律库负责日常生产调用
```

自己的历史文章不再作为主要规律发现对象，只作为验证规律是否适合本账号的实验数据。

## 顶层导航

五个功能区只是导航和归属，不是新建五套数据库。

| 功能区 | 主要承载内容 |
| --- | --- |
| 00｜首页 | 收益指标、当前实验、唯一生产入口、待办视图 |
| 01｜生产系统 | MASTER、内容路由、结构执行、生产协议、表达、QA、终检 |
| 02｜知识系统 | 结构库、ACTIVE规律库、参数库、案例库、平台规则 |
| 03｜运行系统 | 06｜知乎生产卡、当前正文、生产状态、发布状态 |
| 04｜复盘系统 | 04内容资产库、05单篇复盘库、05.5规律验证库、收益数据 |

权威归属以 [`docs/知乎OS权威归属表.md`](docs/知乎OS权威归属表.md) 为准。

## 五句话原则

```text
00 首页
决定做什么
↓
01 生产系统
决定怎么做
↓
02 知识系统
提供能力
↓
03 运行系统
记录正在做什么
↓
04 复盘系统
决定以后怎么做得更好
```

任何页面都必须回答：我属于哪一句。回答不了，说明放错地方。

## 生产卡唯一执行对象

`06｜知乎生产卡` 是唯一日常执行对象。

当前正文、生产状态、发布状态都围绕生产卡记录；不新增 Run 数据库，不新建第二套任务系统。

## 运行模式纪律

知乎OS默认分为两种模式，生产和系统研发必须分离。

模式一：日常生产（默认）

目标：最快速度产出高质量正文。

```text
问题
↓
主变量
↓
一句核心机制
↓
正文
↓
发布
↓
记录数据
```

日常生产过程中禁止修改协议、禁止新增参数、禁止优化 Prompt、禁止重构 Notion。

模式二：系统升级（条件触发）

仅满足以下任一条件时启动：

1. 连续 3 篇出现同一种问题。
2. 数据验证证明某个规律失效。
3. 新发现跨题型通用规律。

除此之外，一律禁止修改系统。所有系统升级必须遵守：先验证，再抽象。

## 五层架构

L1 协议层（Git/MD）
回答：AI 应该怎么干。

L2 知识层（Notion 管理源 / runtime 执行快照）
回答：AI 依据什么干。

L3 执行层（Codex）
回答：今天具体干什么。

L4 平台层（知乎/头条）
回答：真实反馈是什么。

L5 学习层（Notion复盘）
回答：下次如何优化。

## 三层数据源

L1｜平台样本
回答：知乎平台上什么变量正在高频生效。

L2｜账号实验
回答：这些平台变量证据在本账号是否有效。

L3｜ACTIVE变量
回答：哪些已验证变量允许进入 Production Card。

唯一内容变量权威源是 `production_variable_library.md`，显示名称为《知乎内容变量参数库》。日常生产只调用该库中 `当前状态=ACTIVE` 且 `是否允许生产调用=是` 的变量。L1 和 L2 只在系统升级、规律验证和周期复盘时回写同一变量记录。

## 工作流程

截图
↓
Codex
↓
读取总控协议
↓
读取 runtime ACTIVE 执行快照
↓
读取账号画像快照
↓
执行历史资产检索
↓
生成 Production Card
↓
交付可复制 Production Card 与 26 维参数调用审计
```

正文生产只在用户明确要求“生产正文”“写回答”“调用正文节点”“按完整链路生产”时继续：

```text
Production Card
↓
读取知乎正文推理协议 V1.0
↓
Claude 按卡生成正文
↓
运行正文推理校验
↓
发布
↓
抓取数据
↓
本地记录
↓
周期复盘
↓
回写 Notion 管理源并发布下一版 runtime

## 账号画像

账号画像是长期低频数据层，用来指导选题和复盘，不直接改 Prompt，不新增 Production Card 字段。

```text
账号画像（长期）
↓
选题
↓
Production Card
↓
正文
↓
收益数据
↓
L0-L3复盘
```

高频使用两项：

1. 转化内容排行：每月更新新增关注 TOP20，用于建立关注转化排行榜，异常样本进入 L3 深度事实包。
2. 兴趣分布：季度或半年更新，用于校准选题方向。

其它画像字段，包括性别、年龄、地域、关注来源和活跃分层，作为账号基线画像，不参与每篇正文复盘。

## 正式生产完成定义

收到以下任一输入时，系统自动进入正式生产模式：

1. 一个知乎问题。
2. 一个或多个知乎问题截图。
3. “生成卡”“生产卡”“按截图生成卡”“继续”等明确卡片生产指令。
4. 用户要求 Codex 自行去知乎采集、挑选或推荐今日可回答问题。

生产入口分为两类：

```text
用户输入型：用户发问题 / 截图 / 链接
系统自采型：Codex 从知乎入口采集候选并推荐一题
```

两个入口只影响选题来源。题目选定后，后续 Production Card 主链完全一致。

日常正式生产任务的默认完成标准是生成可复制 Claude版 Production Card，并附 26 维参数调用审计：

```text
问题识别
↓
选题准入
↓
读取 runtime/ACTIVE_MANIFEST.md
↓
执行 Skill000 历史资产检索
↓
读取 MANIFEST 指定的 ACTIVE 结构、规律和质量参数
↓
内容路由
↓
ACTIVE 规律回流调用
↓
26 维质量参数总审计
↓
生成 Production Card
↓
执行 validate_production_card.py
↓
交付可复制 Production Card
↓
写入本地运行日志
```

Production Card 是日常生产默认交付对象。除非用户明确要求生产正文，不得自动交给 Claude 正文生产节点。

完整正文链路触发词：

- 写文案
- 生产正文
- 写回答
- 调用正文节点
- 按完整链路生产
- 根据这个问题回答

## 模式路由

正式生产模式触发词或输入：

- 生成卡
- 生产卡
- 按截图生成卡
- 继续
- 用户发送知乎题目或题目截图并要求生产

行为：自动生成可复制 Production Card，并附 26 维参数调用审计；不自动生产正文。

完整正文模式触发词：

- 写文案
- 生产正文
- 写回答
- 调用正文节点
- 按完整链路生产
- 根据这个问题回答

行为：在 Production Card 校验通过后继续调用正文生产、推理校验和 QA。

审计模式触发词：

- 检查流程
- 审计系统
- 检查 Production Card
- 只生成生产卡
- 不写正文
- 检查参数调用

行为：只执行用户指定的审计范围，不自动调用 Claude 生成正文。

禁止因为输入是截图就默认进入审计模式。判断依据是用户意图，不是输入形式。

所有执行协议以 `/docs` 为准；Codex 日常生产知识以 `runtime/ACTIVE_MANIFEST.md` 列出的 ACTIVE 快照为准。Notion 是管理权威，负责审批和发布下一版 runtime。

## 生产接口

日常正文生产必须经过 `Skill006｜知乎生产卡生成器`。

Codex 负责读取 runtime、选择变量、压缩知识并输出固定格式 Production Card。

Trigger 判断当前仅为 Skill006 内部能力提案（PROPOSAL），不是独立系统或独立入口，不参与生产运行；系统唯一生产入口仍是 Skill006｜知乎生产卡生成器。

Claude 只负责根据 Production Card、`知乎正文推理协议 V1.0` 和 `知乎正文表达协议 V3` 写正文，不直接读取 Notion 或 runtime，不新增变量，不修改核心机制，不虚构案例。

GPT / Codex 不直接生产文案，不重写正文。日常默认只交付可复制 Production Card；完整正文模式下只负责执行编排、校验、QA 判定、生成修正指令和交付通过 QA 的 Claude 正文。

## 正文推理协议

`docs/知乎正文推理协议 V1.0.md` 位于 Production Card 与 Claude 正文生产 Prompt 之间。

它只约束正文生成前的推理过程，不新增 Production Card 字段，不修改结构库，不修改参数库，不修改表达协议。

## 正文表达协议

`docs/知乎正文表达协议 V3.md` 位于正文推理协议与 Claude 正文生产 Prompt 之间。

它只约束正文怎么说出来，不决定写什么，不新增变量，不修改核心机制，不替代 QA。

Claude 正文生成后，先运行 `scripts/validate_reasoning.py`，再进入 Skill007 正文 QA。

## 正文 QA

`Skill007｜正文 QA 协议` 负责审核 Claude 正文是否遵守 Production Card。

Skill007 只审核，不修改正文，不重写正文，不增加新观点，不修改 Production Card。

Skill007 V1.0 已进入冻结期：连续生产 20 篇知乎正文后，再根据 QA 数据决定是否升级 V1.1。
