# CHANGELOG

## 2026-08-05｜Milestone M4：内容架构冻结（Content Architecture Freeze）

状态：Completed

### 变更

- 新增《内容架构总则》并冻结为 `ARCHITECTURE_ACCEPTED`，正式定义内容语义四层：现实、认知落差、认知转换、平台表达。
- 明确与知乎OS Compiler 的职责边界：内容架构总则负责 Why（为什么这样生成），Compiler 负责 How（怎么生成），两者不构成双重权威。
- 确立 Layer（层级）与 Object（对象）分离治理原则：发现新规律先归位为对象，只有多次验证后才允许升级层级，不再"发现即新增层级"。
- 确立 Runtime 与 Knowledge 解耦原则：知识库可无限增长，Runtime 只读取认知转换卡这一最小输入，不直接读取整个知识库。
- 确立跨平台共享内核：现实、认知落差、认知转换三层与平台无关，知乎、头条等多平台差异只落在"平台表达"这一层，不需要为每个平台维护独立分析系统。

### 为什么改

系统此前的链路是"治理 → Compiler → Writer"，Compiler 只回答怎么生成，不回答为什么这样生成，导致语义判断（现实是什么、主落差是什么、认知转换是什么）分散在 Analyzer、Slim IR 等工程对象里，没有独立的语义层可以被审查和复用。今天补上"治理 → 内容架构总则 → Compiler → Writer"这一环，是架构升级，不是单纯增加文档。

### 影响范围

- `docs/内容架构总则.md`（新增）
- `README.md`
- `docs/知乎OS Compiler V1.md`
- `docs/系统治理原则.md`

### 后续验证

下一阶段不再研究架构，转向验证：用真实选题持续回答三个问题——现实提取对不对、主认知落差选得准不准、认知转换能否带来更好的传播效果。连续多篇验证有效后才允许升级《内容架构总则》；验证不通过时，只修改对象和规则，不再改动顶层架构。

## 2026-07-31｜V1.7

### 变更

- 新增 `Reasoning First｜推导优先原则` 为系统一级设计原则。
- 明确系统建设顺序：Reasoning Grammar → Evidence Schema → Knowledge Engine → Parser → Automation。
- 明确 Production Card 不直接消费案例，只消费已经沉淀为知识的结果。
- 在权威归属表中登记 Reasoning First 的唯一出处为 `docs/00-设计原则.md` 原则0。
- README 核心原则补充 Reasoning First。

### 为什么改

协议规定怎么执行，架构原则规定为什么这样设计。

推导语法决定内容深度、阅读体验和认知奖励；字段、解析器和自动化只能承载或执行推导，不能替代推导。

### 影响范围

- `docs/00-设计原则.md`
- `docs/知乎OS权威归属表.md`
- `README.md`

### 后续验证

新增 Evidence Schema、Parser、自动化采集或 Production Card 字段前，必须先证明其建立在稳定、可复用的 Reasoning Grammar 之上。

## 2026-07-28｜V1.4

### 变更

- 明确“同题前三高赞回答”不是日常 Production Card 生成硬门槛。
- 日常生产以系统参数为准：选题识别、Notion 变量命中、ACTIVE 结构、质量参数库和相邻样本爆款规律。
- 更新 `Skill006｜知乎生产卡生成器`，没有同题前三高赞回答不得单独作为停止原因。
- 更新 `08｜总AI执行中心`，把同题前三高赞回答定位为复盘增强项和竞品分析项。

### 为什么改

当前系统参数库、变量库、结构库和质量参数库已经足够支撑日常生产。

强制采集同题前三高赞会阻塞连续生产，不符合当前阶段“先跑通生产闭环，再用数据复盘升级”的目标。

### 影响范围

- `skills/Skill006_知乎生产卡生成器.md`
- `docs/08_总AI执行中心.md`

### 如何回滚

- 恢复同题前三高赞回答为正式生产前强制节点。
- 没有同题高赞样本时停止 Production Card 生成。

## 2026-07-27｜V1

### 变更

- 新增五层架构：协议层、知识层、执行层、平台层、学习层。
- 明确 Git/MD 负责稳定协议，Notion 负责动态知识和学习复盘。
- README 收敛为系统入口，不承载详细说明。
- 总控提示词改为可复制即用的 MASTER。
- 新增协议升级标准：只有满足四项条件才允许修改 MD 协议。
- 新增 `docs/00-设计原则.md`，作为所有协议的共同设计边界。

### 为什么改

原系统中协议、知识、复盘和执行边界容易混在一起。

如果日常反馈直接推动协议变化，系统会出现频繁改 Prompt、改 Router、改总控的问题，导致执行不稳定。

### 证据

- 总控提示词文件曾混入历史讨论内容，无法直接作为 MASTER 使用。
- README 容易从入口扩展成系统说明书。
- ACTIVE 结构同时具备协议属性和知识属性，需要明确：固定路由可在 MD，验证型结构知识应在 Notion。

### 影响范围

- `README.md`
- `templates/知乎OS总控提示词.md`
- `docs/知乎OS执行协议.md`
- `docs/00-设计原则.md`

### 如何回滚

- 恢复 README 为原推荐使用说明。
- 恢复总控提示词为变更前内容。
- 从执行协议中移除五层职责、双循环和协议升级标准。
- 删除 `docs/00-设计原则.md`。

### 后续验证

接下来不继续扩展协议设计。

优先让 Codex 连续执行真实选题生产，用平台数据验证五层架构是否稳定。

## 2026-07-27｜V1.1

### 变更

- 将 Notion 知识调用从“可选增强”改为“正式生产硬门槛”。
- 总控提示词新增 Notion 检索、命中页读取、知识调用审计和失败停止规则。
- 执行协议新增“知识调用硬门槛”。
- 禁止在未读取 Notion 时降级为本地 TOP10 事实包、通用经验或模型记忆继续生成正文。

### 为什么改

一次真实截图生产中，Codex 输出了“未读取 Notion，仅基于当前可见信息和本地 TOP10 事实包执行”。

这说明协议生效了，但知识层没有实际参与执行，导致文案更像通用 ChatGPT 生成，而不是基于知乎OS知识库生产。

### 证据

- Notion 连接实际可用，workspace 为“黄 升的工作空间”。
- 同题及相邻管理题能检索到多个 Notion 命中页。
- 命中页包含“管理强下属不是压制能力，而是让授权、结果责任、边界和收益分配同时对齐”等可调用知识。
- Notion 路由协议中已有“工具读取失败属于ERROR/RETRY，不得伪装成业务STOP”的约束，但本地 MD 未强制同步。

### 影响范围

- `templates/知乎OS总控提示词.md`
- `docs/知乎OS执行协议.md`

### 如何回滚

- 将 Notion 调用恢复为可选增强。
- 删除“知识调用失败”停止模板。
- 允许未读取 Notion 时基于本地事实包继续生产。

## 2026-07-27｜V1.2

### 变更

- 将“同题前三高赞回答分析”从单次生产硬门槛改为复盘增强项。
- 日常生产只强制要求读取 Notion 知识层，并从相邻样本、TOP变量、选题库和历史收益证据中提取爆款规律。
- 只有用户明确要求严格竞品分析，或提供同题高赞样本时，才执行同题前三高赞回答分析。

### 为什么改

同题前三高赞样本采集成本高，会阻塞日常内容生产。

当前阶段优先验证 OS 的连续生产能力和数据闭环，不应让竞品分析成为每篇内容的前置瓶颈。

### 影响范围

- `templates/知乎OS总控提示词.md`
- `docs/知乎OS执行协议.md`

### 如何回滚

- 恢复“同题前三高赞回答分析”为正式生产前强制节点。
- 没有同题前三高赞样本时停止正文生产。

## 2026-07-27｜V1.3

### 变更

- 新增知乎内容数据四层职责：L0 内容资产总账、L1 分层样本、L2 变量层、L3 深度事实包。
- 明确四层不是四个数据量，而是四种数据职责。
- 新增每层进入条件、停止条件和禁止越级升级规则。
- 明确 L0 不能直接升级到 L3，必须经过 L1 抽样和 L2 变量提取。
- 规定全量内容采集默认只做 L0 轻采集，不自动扩展为正文、评论、同题高赞或深度复盘采集。

### 为什么改

216 条历史内容不适合全部做深度采集。

如果看到某篇数据好就直接补正文、补评论、补同题高赞，系统会重新滑向“哪个数据好就无限分析哪个”的资料仓库模式。

四层职责用于把采集成本和信息价值对齐：全量索引低成本覆盖，分层样本保留对照，变量层验证差异，深度事实包只服务证据链和决策。

### 证据

- 当前内容管理页显示历史内容约 216 条。
- TOP10事实包已经证明深度采集成本高，不适合扩展到全部历史内容。
- 变量提取批次已显示组织视角、首屏认知校正、身份对立、风险传导等变量需要通过样本对照验证，不能只从 TOP 样本推出。

### 影响范围

- `docs/知乎OS执行协议.md`
- `docs/00-设计原则.md`

### 如何回滚

- 删除执行协议中的“内容数据四层职责”。
- 删除设计原则中的“数据分层按职责”和“禁止低层数据越级形成结论”。

### 后续验证

先建立 216 篇 L0 内容资产总账。

再按高收益、高阅读、高收藏、低阅读高收益、最近7天和异常样本抽取 20 到 30 篇进入 L1/L2。

只有变量具有研究价值并需要支撑决策时，才进入 L3 深度事实包。

## 2026-07-27｜V1.4

### 变更

- 为内容数据四层补充唯一产出物：内容资产总账、分层样本清单、变量标注记录、深度事实包。
- 新增 L0→L1、L1→L2、L2→L3 的升级触发器。
- 将“全量内容执行 L0 轻采集”改为“全量采集默认仅建立内容资产索引”。
- 明确正文、评论、同题高赞等高成本数据必须由 L1/L2/L3 升级流程触发。

### 为什么改

职责说明只能告诉 AI 每层做什么，但不能保证 AI 把结果写到正确位置。

唯一产出物用于防止 L2 变量数据写入 L0，总账混入分析结论，或 L3 深度证据被自动扩采。

升级触发器用于让 AI 在执行时直接判断是否增加采集深度，而不是每篇都临时询问是否分析。

### 影响范围

- `docs/知乎OS执行协议.md`
- `docs/00-设计原则.md`
- Notion：知乎数据复盘协议 V1.0

### 后续验证

执行 216 篇内容资产采集时，只建立 L0 内容资产索引。

采集完成后按抽样规则生成 L1 分层样本清单，再决定哪些样本进入 L2 变量标注。

## 2026-07-29｜V1.7

### 变更

- 新增参数状态治理协议：ACTIVE / LOW-FREQ / OBSERVE / MERGED / DEPRECATED。
- 明确低频不等于低价值，参数不得仅因调用次数低被淘汰。
- 参数淘汰必须依据适用机会、调用命中、结果贡献和能力重叠。
- Skill006 参数审计新增“适用但未调用的路由风险”口径。
- README 将日常参数审计口径从 17 维对齐为 18 维。

### 为什么改

低频参数可能是无价值，也可能是最近没有适用题，或者路由漏调。

只按调用次数治理，会误删安全边界、反例检查、决策升级条件等低频但关键的参数。

### 影响范围

- `docs/知乎内容质量参数库_V2.md`
- `runtime/知乎内容质量参数快照.md`
- `skills/Skill006_知乎生产卡生成器.md`
- `README.md`

### 后续验证

每 20 篇或每月审计一次参数状态。连续两个复盘周期无独立贡献的参数，先进入 OBSERVE；完成对照验证后，才允许转为 MERGED 或 DEPRECATED。

## 2026-07-27｜V1.5

### 变更

- 将知乎 L0 协议标记为 V1.0 / LOCKED。
- 明确 216 篇历史内容采集完成前禁止新增 L0 字段。
- 新增 L0 自动检查要求：内容类型分布、关键字段 NULL 统计、NULL 来源校验、重复 ID、数值可排序。
- 明确内容类型天然缺字段时必须留空并记录 notes，禁止继承上一条或下一条数据。

### 为什么改

第一页试采发现“想法”没有收藏字段，如果程序不受协议约束，容易把下一条内容的收藏数误带入当前记录。

这说明 L0 必须由协议决定数据边界，而不是由页面解析结果临时决定字段含义。

### 影响范围

- `docs/知乎OS执行协议.md`
- `docs/00-设计原则.md`
- L0 验收脚本输出

### 后续验证

继续完成 216 篇 L0 内容资产总账。

全量完成后先输出 L0 统计报告，再由程序生成 L1 分层样本清单。

## 2026-07-29｜V1.6

### 变更

- 新增 `docs/知乎正文表达协议 V3.md`。
- 将正文链路拆清楚：Production Card、正文推理协议、正文表达协议、Claude 正文生产 Prompt、QA。
- `templates/Claude正文生产Prompt.md` 新增 Expression Protocol 必填输入。
- `docs/知乎OS执行协议.md` 和 `templates/知乎OS总控提示词.md` 明确禁止跳过表达协议直接生成正文。
- README 补充正文表达协议入口说明。

### 为什么改

现有正文 Prompt 偏执行约束，能避免跑题、虚构和变量漂移，但缺少正向表达策略。

结果是正文容易变成“正确答案”，而不是具有阅读惯性、场景感和判断工具的知乎回答。

### 核心策略

- 先讲现象，不先讲概念。
- 机制必须落到具体动作。
- 解释一句，推进一句。
- 每段回答一个隐含问题。
- 抽象后立即举组织场景。
- 结尾不是总结，是工具。

### 影响范围

- `docs/知乎正文表达协议 V3.md`
- `templates/Claude正文生产Prompt.md`
- `docs/知乎OS执行协议.md`
- `templates/知乎OS总控提示词.md`
- `README.md`

### 如何回滚

- 删除 `docs/知乎正文表达协议 V3.md`。
- 从 Claude 正文生产 Prompt 中移除 Expression Protocol 必填要求。
- 从执行协议和总控提示词中移除表达协议调用节点。
