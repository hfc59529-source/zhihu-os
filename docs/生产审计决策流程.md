# 生产审计决策流程 V1.3

版本：AUDIT-DECISION-FLOW-V1.3

状态：ACTIVE

## 核心原则

选题采集、生产卡生成、正文生成、QA、审计、决策、修复、修改后确认、用户验收和发布必须职责分离，避免同一方既发现问题又给答案又自己拍板。

Codex 只负责选题采集与入库。Codex 不写 Production Card，不写正文，不做 QA，不写审核记录、决策记录、发布记录，也不得直接改写 `Draft-v1.md`、`Article-v*.md`、`Article-Patched-v*.md`、`Article-Final.md` 或 `Release-v*.md` 的正文段落。

## 职责表

| 阶段 | 负责人 | 输出 |
|---|---|---|
| Topic Package | Codex | data/topic_candidates/YYYY-MM-DD/TOPIC-YYYYMMDD-NNN.md |
| Production Card | Claude | Card-v1 |
| Draft Production | Claude | Draft-v1 |
| QA | Claude / 后续生产角色 | QA结果 |
| Audit | GPT / 人工 | Audit_Report |
| Decision（仅 Audit 有 Issue 时） | GPT | Approve / Reject / Revise |
| Patch | Claude（依据 Decision） | Article-Patched-v1 / Patch.diff |
| Apply Patch / Archive | Claude / 后续生产角色 | 变更归档、状态记录 |
| 修改后确认（仅发生 Patch 后） | GPT / 人工 | 修改有效确认 |
| User Review | 用户 | User_Review_Package / USER_APPROVED / USER_REJECTED |
| Release | Claude / 用户 | Release-v1 |
| Publish | 用户 / 后续生产角色记录 | 发布链接、发布时间 |

## 受保护正文文件

以下文件只能由 Claude / 写作角色产出或修正：

- `Draft-v1.md`
- `Article-v*.md`
- `Article-Patched-v*.md`
- `Article-Final.md`

`Release-v*.md` 是发布稿容器，不是修正文档。Codex 只有在用户验收通过后，才允许从已验收正文同步生成 `Release-v*.md`；不得在 Audit / Patch / Patch Validation 阶段手写或润色其中的正文段落。

## Codex 编辑前检查

Codex 每次准备编辑文件前，必须先做三项判断：

1. 目标文件是否属于受保护正文文件。
2. 当前状态是否允许 Codex 写该文件。
3. 本次动作是否包含新增、删除、替换、润色正文句子。

只要任一项触发正文写作风险，Codex 必须停止正文编辑，改为更新 `Audit_Report.md`、`Decision_Log.md` 或状态文件，并交接给 Claude / 写作角色。

## 铁律：审计必须基于可验证证据，不得基于个人写作偏好提出修改建议

Audit_Report 中的 Problem / Risk 只能基于以下可验证类别：

- 重复（同一语义在短距离内复现）
- 跳跃（逻辑链缺失中间环节）
- 逻辑断裂（因果不成立）
- 证据不足（关键判断无支撑）
- 与 Production Card 不一致
- 阅读障碍（语法/指代/结构导致理解成本过高）
- 事实风险（涉及未经核实的承诺、数据、政策）

**禁止**出现"感觉更顺""我更喜欢""读起来舒服""建议更有力量"这类风格偏好表述。否则 Claude 和 GPT 会各自倾向不同文风，系统判断标准会逐渐漂移，失去可复现性。

## 完整闭环

```text
Codex
│
├── Production Card（冻结内容权威）
│
▼
Claude
│
├── Draft-v1（冻结）
│
▼
Codex
│
├── QA（只审 Card 一致性，不修改正文）
│
▼
GPT / 人工
│
├── Audit PASS
│   ▼
│   User Review（用户阅读最终正文并确认是否可发布）
│
└── Audit Issue
    ▼
GPT
│
├── Approve
├── Reject
└── Revise
│
▼
Claude
│
└── Article-Patched-v1 / Patch.diff（GPT Approve/Revise 之后才写，不能提前写）
│
▼
Apply Patch / Archive（Codex 执行）
│
▼
GPT / 人工确认修改有效（只在发生 Patch 后执行）
│
▼
User Review（用户阅读最终正文并确认是否可发布）
│
├── USER_REJECTED
│   ▼
│   Decision（根据用户指出的问题裁决是否修复）
│   ▼
│   Claude Patch → Codex 复核 → GPT / 人工确认修改有效 → User Review
│
└── USER_APPROVED
    ▼
Release（用户验收后生成 / 确认发布稿）
│
▼
Production Ledger（记录 + Decision_Log 归档）
```

## 关键规则

1. **Claude 不提前写 Patch**。Audit_Report 提交后必须等 GPT 输出 Approve / Reject / Revise，Claude 才能动笔写 Patch.diff 或 Article-Patched-v1。提前写好的 Patch 等于把结论塞给 GPT，破坏独立判断。
2. **Codex 不写 Patch 正文**。Codex 发现问题时只能定义 Issue、证据、风险、修正目标和下一状态，不能直接替 Claude 改正文。任何由 Codex 直接改写正文并推进复检的行为都视为流程事故。
3. **Draft 冻结**。Draft-v1.md 一旦生成不可被直接覆盖，任何改动只能以 Patch 或 Article-Patched 形式归档，并经过审核链路。
4. **Decision 只在 Audit 有 Issue 时执行**。Audit PASS / Issue = 0 时，不需要空跑 Decision，不需要为证明流程完整而制造裁决记录。
5. **每篇 Production 必须有 Decision_Log.md**，记录每个 Issue 的 Claude 判断、GPT 判断、最终决定、负责人、原因。这比 Patch 本身更重要，是未来回溯"为什么这么改"的唯一依据。
   若 Audit PASS / Issue = 0，Decision_Log 可记录 `Issue = 0 / No Decision Required`，不得强制生成空裁决。
6. **修改后确认只在发生 Patch 后执行**。无问题正文在 Audit PASS 后直接进入 `READY_FOR_USER_REVIEW`；有问题正文在 Claude Patch、Codex 复核后，由 GPT / 人工确认修改是否有效，再进入 `READY_FOR_USER_REVIEW`。
7. **RELEASE_READY 必须在用户验收之后**。Audit PASS 或修改后确认通过只表示审核条件满足，不表示用户已经看过正文，也不表示可以直接发布。
8. **进入 Release / Publish Queue 的唯一入口是 USER_APPROVED**。只有用户明确 `USER_APPROVED` 后，Codex 才能生成或确认 `Release-v1.md`，并将状态更新为 `RELEASE_READY`。
9. **USER_REJECTED 返回路径**。用户拒绝后，必须根据用户指出的问题进入 Decision；若需要修复，由 Claude 修复正文，再回到 Codex 复核、GPT / 人工确认修改有效和 `READY_FOR_USER_REVIEW`。Codex 不得借用户拒绝直接改写正文。
10. **用户验收必须有上下文包**。进入 `READY_FOR_USER_REVIEW` 时，Codex 必须提供 `User_Review_Package.md` 或等价展示，包含 Production ID、原知乎问题、真实问题链接、当前正文版本、当前状态和完整正文内容。正文文件可以是纯正文，但不得把无头正文直接交给用户验收。
11. **Release 不能前置替代验收包**。未获得 `USER_APPROVED` 前，不得把 `Release-v1.md` 当作用户验收包、正式发布稿或 Publish Queue 入队依据。

## Audit_Report 标准格式

每个问题独立编号：

```text
Issue-01

位置：
（行号或段落定位）

严重等级：
Critical / Major / Minor

Evidence：
（原文引用）

Problem：
（发现的问题是什么）

Risk：
（如果不处理，可能的后果）

Suggestion：
（Claude 的倾向性意见，仅供参考，不是结论）

Need GPT Review：
YES
```

GPT 审核时只需对每个 Issue 输出：

```text
Issue-01：Approve / Reject / Revise
Issue-02：Approve / Reject / Revise
...
```

## Decision_Log 标准格式

```text
Issue-01

Claude：
（Claude 的判断和理由）

GPT：
（GPT 的判断和理由）

最终：
保留 / 修改为 ___

负责人：
（用户姓名或"系统默认"）

原因：
（为什么最终这么定）
```

## 版本控制

| 版本 | 日期 | 说明 | 状态 |
|------|------|------|------|
| V1.0 | 2026-08-01 | 初版：Draft冻结 + Audit先行 + GPT审Audit + 后写Patch + Final Validation + Decision Log | ACTIVE |
| V1.1 | 2026-08-01 | 统一职责：Codex 负责 Card / QA / 状态归档，Claude 负责正文生产和修复，GPT/人工负责 Audit / Decision / Validation，用户验收后才进入 Release | ACTIVE |
| V1.2 | 2026-08-01 | 新增 Codex 正文禁写边界、受保护正文文件、编辑前检查，防止 Codex 越权执行正文 Patch | ACTIVE |
| V1.3 | 2026-08-01 | 简化流程：Audit PASS 直接进入 User Review；Decision 和修改后确认只在有 Issue / Patch 时执行 | ACTIVE |

**执行说明**：本版本用于消除正文生产负责人和 RELEASE_READY 定义冲突；后续暴露同类问题再修改协议，若只是个别问题，保持流程稳定，不继续推演设计。
